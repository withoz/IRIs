# SketchUp 프로브 — Phase 0-2 과제 4

SketchUp Ruby API로 지오메트리를 덤프해 **라이브 링크 실현성**을 판정합니다.
Phase 0의 산출물은 코드가 아니라 의사결정이므로, 실행 결과를 아래 "측정 결과"에 기록하십시오.

## 실행

SketchUp 2026에서 모델을 연 뒤 **창 > Ruby 콘솔**을 열고:

```ruby
load 'E:/IRIS/tools/sketchup/iris_probe.rb'
IRIS::Probe.run
```

| 호출 | 용도 |
|---|---|
| `IRIS::Probe.scan` | **규모 사전 조사.** 지오메트리 버퍼를 만들지 않아 빠름 — 항상 이것부터 |
| `IRIS::Probe.run` | 측정 + JSON 덤프 (`out/sketchup/<모델명>.iris.json`) |
| `IRIS::Probe.run(dump: false)` | 측정만. 파일 쓰기 생략 |
| `IRIS::Probe.run(limit: 200_000)` | 삼각형 예산. 초과 시 중단하고 부분 집계 출력 |
| `IRIS::Probe.run(pretty: true)` | 사람이 읽을 수 있게 들여쓴 JSON |
| `IRIS::Probe.watch` | 변경 감지 옵저버 부착 |
| `IRIS::Probe.flush` | 부착 이후 쌓인 델타 출력 후 비움 |
| `IRIS::Probe.unwatch` | 옵저버 해제 |

### 대형 모델 순서

**`run`은 면마다 `Face#mesh`를 호출하므로 실제 프로젝트 모델에서는 수 분이 걸릴 수 있습니다.**
Ruby가 메인 스레드를 점유해 SketchUp UI가 완전히 얼어붙고, **중단할 방법이 없습니다**
(강제 종료 외). 반드시 아래 순서로 진행하십시오.

```ruby
IRIS::Probe.scan                              # 1. 규모 파악 (빠름)
IRIS::Probe.run(dump: false, limit: 200_000)  # 2. 처리량 측정 (예산 제한)
IRIS::Probe.run                               # 3. 전체 덤프 (1·2 결과 보고 판단)
```

진행 상황은 SketchUp **상태바**에 표시됩니다 (`Sketchup.status_text`). Ruby 콘솔 출력은
작업이 끝나야 갱신되므로 상태바가 유일한 실시간 피드백입니다.

스크립트를 수정한 뒤에는 `load`를 다시 호출하면 그대로 갱신됩니다.

### 증분 동기화 테스트 절차

```ruby
IRIS::Probe.watch
# → SketchUp에서 박스 하나 그리기 / 옮기기 / 지우기
IRIS::Probe.flush
```

`watch`는 **최상위 `model.entities`에만** 옵저버를 붙입니다. 그룹·컴포넌트 내부 편집까지
잡으려면 각 `Entities`에 개별 부착이 필요하며, 이 범위 문제 자체가 라이브 링크 설계의
난점 중 하나이므로 결과를 기록해 두십시오.

## 출력 JSON 구조

렌더러가 바로 쓸 수 있는 2단 씬 그래프입니다. **정의(메시) / 인스턴스(변환)** 분리가 핵심이며
이것이 그대로 BLAS / TLAS 구조에 대응합니다.

```
definitions : { def_<id>: { meshes[], children[], persistent_id, instance_count } }
root        : { meshes[], children[] }
materials   : [ { id, name, color, alpha, type, texture } ]
```

- `meshes[]`는 **머티리얼별로 분리**되어 있습니다 = 드로우콜 / 지오메트리 분리 단위
- `positions`는 **미터**. SketchUp 내부 단위(인치)에서 변환됨
- 좌표계는 SketchUp 원본 유지 — **Z-up, 우수 좌표계**. Y-up 변환은 렌더러 임포터 책임
- `transform`은 16개 float, **열 우선(column-major)**, 이동 성분은 인덱스 12·13·14

## 확인할 것

| # | 항목 | 판정 기준 |
|---|---|---|
| 1 | 정점·법선·UV 추출 | 셋 다 `O`여야 렌더러 입력으로 충분 |
| 2 | `persistent_id` | `O`가 아니면 증분 동기화 불가 → 라이브 링크 설계 재검토 |
| 3 | 인스턴스/정의 재사용률 | 1.0에 가까우면 인스턴싱 이득 없음 → 실제 BIM 모델로 재측정 |
| 4 | 추출 처리량 | 1000만 삼각형 환산 시간이 최초 로딩 비용의 하한 |
| 5 | 삭제 델타 | `onElementRemoved`가 entityID만 준다는 점 확인 |

## 알려진 제약

- **삭제 콜백은 `entityID`만 전달합니다.** 엔티티가 이미 무효라 `persistent_id`를 조회할 수 없습니다.
  → 렌더러 측에서 `entityID → persistent_id` 매핑 테이블을 자체 유지해야 합니다. 라이브 링크
  아키텍처에 직접 영향을 주는 제약입니다.
- 이 프로브는 **Ruby API만** 사용합니다. 자료집 10.2가 지적한 대로 실제 제품에서는
  지오메트리 추출을 C API(SketchUp SDK)로 옮겨야 할 가능성이 높으며, 그 판단 근거가
  여기서 나오는 처리량 수치입니다.
- SketchUp 2026의 임베디드 Ruby는 **3.2**입니다 (`x64-ucrt-ruby320.dll`).

## 측정 결과

| 측정일 | 모델 | 삼각형 | 정의/인스턴스 | 추출 시간 | 법선·UV | persistent_id |
|---|---|---|---|---|---|---|
| 2026-09-07 | 세종 자동차관련시설 멀티브랜드_TH (실제 프로젝트) | 91,762 | 425 / 1,699 | 1,219 ms | O / O | **O** |

환경: WyoungK · SketchUp 26.2.243 · Ruby 3.2.2

| 항목 | 값 |
|---|---|
| 면 | 28,379 (추출 실패 **0**) |
| 정점 | 147,601 |
| 인스턴스 재사용률 | **4.00** 인스턴스/정의 |
| 머티리얼 | 41종 (텍스처 보유 15) |
| 처리량 | 75,308 삼각형/초 |
| 1000만 삼각형 환산 | 132.8 초 |

### 판정

1. **증분 동기화 설계 성립.** `persistent_id`가 정상 동작하고 `EntitiesObserver`·`ModelObserver`
   둘 다 사용 가능합니다. 라이브 링크의 가장 큰 관문을 통과했습니다.
2. **렌더러 입력으로 충분.** 정점·법선·UV 모두 확보되고 28,379면 중 추출 실패가 하나도
   없었습니다. `Face#mesh(1|2|4)` 플래그 조합이 SketchUp 2026에서 유효함이 확인됐습니다.
3. **인스턴싱 이득 실재.** 재사용률 4.00 — BLAS를 425개만 만들고 TLAS에 1,699개를 배치하면
   됩니다. 실제 건축 모델에서 컴포넌트 구조가 살아 있다는 확인입니다.
4. **Ruby 처리량이 병목.** 75k 삼각형/초는 1000만 삼각형 모델에 133초입니다. 최초 1회
   변환이라 해도 대형 BIM에서는 부담이며, 자료집 10.2가 예측한 대로 **지오메트리 추출을
   C API(SketchUp SDK)로 옮겨야 한다는 근거**가 실측으로 확보됐습니다.

### 후속 과제로 확인된 것

- **정점 병합(welding) 없음.** 삼각형 91,762개에 정점이 147,601개입니다. 면 단위로 독립
  버퍼를 만들어 면 경계에서 정점이 중복됩니다. 실제 파이프라인에서는 meshoptimizer 등으로
  후처리가 필요하며, 이 수치가 그 필요성의 근거입니다.
- **삭제 델타의 ID 매핑** — 아래 "알려진 제약" 참조. `watch`/`flush`로 아직 미검증.
- 씬(장면)·카메라·태양 위치는 이 프로브가 수집하지 않습니다. 이 모델만 해도 장면이 20개가
  넘으므로 별도 과제로 다뤄야 합니다.
